def factorial(n):
    if n == 0 or n == 1:
        return 1
    return n * factorial(n - 1)

# Entrada del usuario
num = int(input("Ingresa un número: "))
print(f"El factorial de {num} es {factorial(num)}")
